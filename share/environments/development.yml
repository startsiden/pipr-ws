# configuration file for development environment

# the logger engine to use
# console: log messages to STDOUT (your console where you started the
#          application server)
# file:    log message to a file in log/
logger: "console"

# the log level for this environement
# core is the lowest, it shows Dancer's core log messages as well as yours
# (debug, warning and error)
log: "core"

# should Dancer consider warnings as critical errors?
warnings: 0

# should Dancer show a stacktrace when an error is caught?
show_errors: 1

# auto_reload is a development and experimental feature
# you should enable it by yourself if you want it
# Module::Refresh is needed 
# 
# Be aware it's unstable and may cause a memory leak.
# DO NOT EVER USE THAT FEATURE IN PRODUCTION 
# OR TINY KITTENS SHALL DIE WITH LOTS OF SUFFERING
auto_reload: 0

# whitelisted_hosts: [ 'localhost', '127.0.0.1' ]

allow_local_access: 1

cache_dir: cache

plugins:
    Thumbnail:
        cache: thumb_cache

sites:
  abcn:
     sizes: [ '972x', '680x', '486x', '243x' ]
     allowed_targets: [ 'http://dev.abcnyheter.no', '/files', 'files', 'http://csp.picsearch.com', 'https://abcnyheter.drpublish.aptoma.no/' ]
     prefix: http://dev.abcnyheter.no/

